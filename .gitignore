# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Secrets — never commit real Supabase keys
.env
.env.*
!.env.example

# Supabase CLI local state (project ref, pooler URL) — regenerated by `supabase link`
supabase/.temp
supabase/.branches

# Claude Code runtime state
.claude/scheduled_tasks.lock

# Scraper intermediate output — src/data/sites.json is the merged source of truth
scripts/output/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
